<% if current_user && @owner_view %>
  <%= render partial: 'owner_view', locals: { reservation: @reservation } %>
<% else %>
  <%= render partial: 'customer_view', locals: { reservation: @reservation } %>
<% end %>
<%= render 'errors' %>